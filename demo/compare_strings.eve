# compare strings
driver compare_strings:
    expect('this'  == 'this');   //  true (same value)
    expect("this"  == 'this');   //  true (same value)
    expect(' this' != 'this');  //  true (not same value)
    expect("this " != "this");  //  true (not same value)
return;    
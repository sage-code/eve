# List join() method call demo
driver list_join:

process
  new str := (1,2,3).join(', ');
  print str; -- "1, 2, 3"
return;

/* join() is a method of a list.
   therefore can be call using "." operator */

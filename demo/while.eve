#driver "while"

--example of collection iteration
method main()
  Array: test := ["a","b","c","d","e"]
  Natural: i := 1
start
  while (i <= test.length) do
    element := my_list[i]
    -- shortcut 
    when (element ≥ "c")
       write (element)
       write (',') if (element <> "e")
    ready
  done
finish.